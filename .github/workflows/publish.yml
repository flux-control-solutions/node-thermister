name: Release

on:
  push:
    branches: [main]

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  release:
    runs-on: ubuntu-latest
    # Husky's pre-commit hook runs the local format, lint, and test gate. The
    # only commit in this job is the generated "Version Packages" commit, and a
    # hook failure there stops the release. The steps below are the gate here.
    env:
      HUSKY: '0'
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun run test
      - run: bun run build
      - id: changesets
        uses: changesets/action@v1.9.0
        with:
          version: bun run version
          publish: bun run release
          createGithubReleases: true
